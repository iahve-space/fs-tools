@PACKAGE_INIT@

# Загружаем экспортированные таргеты из install(EXPORT ...)
include("${CMAKE_CURRENT_LIST_DIR}/fs_toolsTargets.cmake")

# Поддержка COMPONENTS и REQUIRED делается этой функцией
check_required_components(fs_tools)